# Linux 物理内存碎片检测与慢路径代价归因工具（v1 + v2）

> 本文沿用 `简历描述.doc` 的叙述风格，把 v1（碎片状态观测）与
> v2（慢路径代价量化）合并成一份完整描述，并补齐原文档里空着的「简历写法」一节。

项目仓库
https://github.com/2862426199qq-beep/linux_eBPF
（v1 原始仓库：https://gitee.com/Lxbai0/PilotGo-plugin-MFD）

---

# ★ 简历定稿（2026-08-14，直接抄这一段）

> 格式对齐原 `简历描述.doc` 的排版。**对外不区分 v1/v2，呈现为一个完整闭环项目。**
> 正文 571 字（原 v1 版约 290 字）。四点字数 167 / 130 / 108 / 85。

**Linux 物理内存碎片化可视化监测工具**　　kernel/eBPF/mem/Zone　　2025.03–2025.05

❖ **项目背景**：Linux 系统长期运行后，物理内存会因频繁的分配与释放产生碎片化。本项目基于 BCC 开发了一款 eBPF 内存碎片监控工具，并量化碎片对分配路径造成的阻塞代价。

❖ **技术要点**：

1. **eBPF 内核态快路径数据采集**：通过 krpobe 和 tracepoint 挂载到两个关键位置：`mm_page_alloc_extfrag` 跟踪点，用于捕获因内存碎片导致分配降级（fallback）的事件；`get_page_from_freelist` 函数入口，用于采集系统所有内存分配请求发生时，伙伴系统各 zone 和各 order 的空闲页面状态。

2. **慢路径阻塞过程采集**：挂载 `try_to_compact_pages` 的入口跟踪点与函数返回做两层配对，采集进程因申请不到连续内存而被同步阻塞的申请阶数、阻塞时长与结局；并挂载 `compaction` 与 `vmscan` 跟踪点，采集双扫描器扫描量、页迁移成败数与直接回收页数。

3. **双重指标与代价量化算法**：实现了外部碎片化指数和不可用空闲指数两种评估算法，从不同维度量化碎片程度；并实现分阶数的对数延迟直方图、以单次分配为粒度的规整成功率、以及判别碎片性质的 free/migrate 双扫描器扫描比。

4. **python curses 终端动态可视化**：利用了 BCC 框架来简化 eBPF 程序的加载和管理工作在用户态，并利用 Python 的 curses 库构建了丰富的终端图形界面（TUI）。

### 定稿时的取舍（改简历前先看这几条）

- **不写任何实测数字**（377 次、1~2 ms、P99 16 ms 等）。实验在个人虚拟机上做，绝对值说服力有限；口吻定位为**通用工具**。数字全在 `报告_P0.md` 里，面试被追问"效果如何"时再拿出来。
- **不出现 v1/v2 字样**，对外是一个完整项目。
- **点 2 砍掉了实现细节**（"以线程号为 key"、"入口取阶数出口取结局"）。简历写"两层配对"即可，细节留给追问。
- **点 1 最重（167 字）是有意的** —— 它是原简历里就最长的一点，快路径是项目地基。

### ⚠️ 与代码现状的两处出入（写进简历前必须先补上代码）

| 简历里的话 | 代码现状 | 怎么补 |
|---|---|---|
| 点 2「挂载 `compaction` 跟踪点，采集**双扫描器扫描量**」 | `compactinfo.c` 把 `migrate_pfn`/`free_pfn` 存进了 `zone_t`，但**有意不据此算扫描量**（决策 #9：扫描器会重启，硬算出负数）。真实来源是 `/proc/vmstat` 的 `compact_migrate_scanned` / `compact_free_scanned` | 在 `extfrag.py` 里读这两个 vmstat 计数器并输出（约 15 行）；或把措辞改成「采集双扫描器进度」 |
| 点 3「实现了…**free/migrate 双扫描器扫描比**」 | 这个计算目前在 **`run.sh`（压力注入器）** 里，**不在 `extfrag.py`（监控工具）** 里 | 同上，挪进 `extfrag.py` |
| 点 2「`vmscan` 跟踪点…**直接回收页数**」 | **P1 尚未开工**，`reclaiminfo.c` 不存在 | P1 阶段完成 |

**结论：这版定稿是 P1 完成后的目标态。** P1 做完 + 上面那 15 行补上，这段话才全部为真。

---

## 项目背景

Linux 操作系统在长期运行后，物理内存会因频繁的分配与释放产生碎片化。即使系统
总空闲内存充足，也可能无法满足大块连续内存的分配请求，从而导致性能下降。传统的
内存监控工具（如 `/proc/buddyinfo`）主要提供静态的文本信息，缺乏直观的可视化展示和
实时监控能力。

本项目**第一阶段（v1）**实现了一款基于 eBPF 的物理内存碎片可视化监控工具：实时采集
内核态的内存分配事件与伙伴系统状态，计算业界公认的碎片化指数，并在用户态终端提供
动态可视化界面。

但 v1 在实际使用中暴露出一个根本局限：**它只回答了"内存有多碎"，没有回答"所以呢"。**
在开发机上实测发现，`/proc/vmstat` 的 `compact_stall`、`pgscan_direct` 与
`/proc/pressure/memory` 的累计阻塞时间**三者全为 0** —— 也就是说，v1 报出的碎片指数，
从来没有真正给任何一个进程造成过可观测的后果。碎片化本身不是问题，**碎片化导致的
分配慢路径开销才是问题**。

因此**第二阶段（v2）**把工具从"碎片状态观测"升级为"**慢路径代价的量化与归因**"：
当碎片把内存分配从快路径（`get_page_from_freelist`）逼进慢路径
（`__alloc_pages_slowpath`）之后，进程被 **direct compaction（直接规整）**
同步阻塞了多久、这次规整的最终结局是什么、内核为此搬运了多少页又失败了多少页 ——
把"有多碎"变成一笔可以量化的**账单**。

---

## 核心技术点

### 一、v1：碎片状态观测

#### 1. eBPF 内核态高效数据采集

该项目利用 eBPF 技术，实现了对内核内存管理事件的零侵入式监控。eBPF 程序被挂载到
两个关键位置：`mm_page_alloc_extfrag` 跟踪点，用于捕获因内存碎片导致分配降级
（fallback）的事件；以及 `get_page_from_freelist` 函数入口（kprobe），用于实时采集
系统所有内存分配请求发生时，伙伴系统各 zone 和各 order 的空闲页面状态。

这种方式避免了修改内核源码，极大地降低了监控开销，并保证了生产环境部署的安全性。
其中 `get_page_from_freelist` 是伙伴系统在**快路径**中尝试从空闲链表分配物理页面的
核心函数，每次内存分配都会经过，因此该模块通过 `last_time_map` 与 `delay_map`
两个 BPF map 实现内核态采样限流，避免高频触发影响系统性能。

#### 2. 双重碎片化指标计算算法

项目实现了两种业界公认的内存碎片化评估算法，从不同维度量化碎片程度：

1. **外部碎片化指数（`extfrag_index`）**：衡量系统无法满足特定大小连续内存分配请求的
   严重程度。当存在足够大的连续空闲块时指数为负值，表示状况良好；当缺乏合适空闲块时
   指数为正值，越高表示碎片化越严重。
2. **不可用空闲指数（`unusable_free_index`）**：反映当前总空闲内存中无法满足特定分配
   请求的比例。当空闲内存为 0 时指数为 1000（完全不可用）；当所有空闲内存都可用于
   请求时指数为 0（完全可用）。

两种指数共同提供了更全面的碎片化视图。由于 eBPF 内核态**不支持浮点运算**，两个指数
均在内核态以「实际值 × 1000」的整数形式计算并传出，在用户态还原为小数展示。

#### 3. Python curses 终端动态可视化

在用户态，项目利用 Python 的 curses 库构建了终端图形界面（TUI）。该界面能以表格形式
实时刷新各内存区域的碎片指数和详细数据，并根据参数动态生成条形图直观展示碎片化程度；
支持多种显示模式（如专注特定 NUMA 节点、仅显示指数等），并通过颜色编码高亮高风险
区域，使得在无图形桌面的服务器环境中也能进行高效监控。

---

### 二、v2：慢路径代价的量化与归因

#### 4. 可复现的碎片压力注入器（工程前提）

观测类工具面临一个先决问题：**在一台"干净"的机器上，探针写得再正确也只能得到空表，
连"代码对不对"都无法验证。** 为此项目实现了一套四档位的物理内存碎片压力注入器
（纯用户态 C 程序与编排脚本，与 eBPF 解耦），用于**可控、可复现地**制造外部碎片与
高阶分配需求：

1. **档位一（外部碎片制造）**：`mmap` 大片匿名内存后逐页触碰使其真实驻留，再以
   **随机顺序**对其中一半执行 `MADV_DONTNEED`。此处「随机」是设计核心 —— 顺序释放
   会让归还的页彼此相邻，被伙伴系统的 `__free_one_page()` 一路向上合并回大块，压力
   完全失效；随机释放使归还页的 buddy 大概率仍被占用，合并在第一步即中断，空闲页只能
   以小块形式挂在低 order 链表上。实测仅 512 MB 即可将 Normal zone 的 order-8
   空闲块从 45 个压到 3 个、order-9 从 12 个压到 3 个。
   实现上选择 `MADV_DONTNEED` 而非 `munmap`，是因为后者每挖一个洞就会切分一次 VMA，
   数万个洞会触及 `/proc/sys/vm/max_map_count` 上限，而前者只做 `zap_page_range()`，
   全程仅维持一个 VMA。
2. **档位二（UNMOVABLE 污染，压力基座的核心）**：用户匿名页的 GFP 标志为
   `GFP_HIGHUSER_MOVABLE`，是**可迁移**的，规整时会被直接搬走 —— 因此单靠档位一只能
   得到"规整成功率很高"的结论，测不到真正的代价。真正钉死 pageblock 的是**内核自身的
   对象**：内核代码中存有指向它们的直接映射地址，内核并未实现其迁移，规整扫描器遇到
   只能绕开。该档通过创建海量线程并使其永久阻塞来批量制造这类对象。
   实测过程中发现开发机 `CONFIG_VMAP_STACK=y`，**内核栈并非通常认为的 order-2 连续块，
   而是由 4 个互不相邻的 order-0 页经页表映射而成**（证据：3000 线程使 `KernelStack`
   增加 48096 kB，同时 `VmallocUsed` 增加 48016 kB，两者几乎完全相等；新增 slab 全部
   落在 `SUnreclaim`，`SReclaimable` 分毫未动）。这一发现使该档位的破坏力强于预期 ——
   分散的 order-0 不可迁移页可能同时毁掉 4 个不同 pageblock 的连续性。
   12000 线程实测使 Normal zone 的 Unmovable pageblock 数量从 287 增至 393。
3. **档位三（高阶分配需求）**：order-0 分配永远不会触发规整，必须有请求方索要连续大块。
   该档通过分批写入 `/proc/sys/vm/nr_hugepages` 扩容大页池 —— 这是一条**同步**的
   order-9 分配路径，写 sysctl 的进程会被真实阻塞在内核中，直接进入
   `try_to_compact_pages()`。
4. **档位四（水位压制）**：叠加内存压力将空闲水位压低，逼进 direct reclaim。

编排脚本内建**空闲块库存自检**：先按
`Σ(order-9 块数 + 2 × order-10 块数)` 换算出"无需任何规整即可直接从空闲链表摘取的
大页数量"，再自动把目标值抬高至库存的 1.4 倍以上，同时以 `MemAvailable` 留出安全余量
防止 OOM。该机制来自一次真实失败的归因：初版目标值 2000 而实测库存 2087，导致
`compact_stall` 全程为 0；根因是内核为用户匿名页分配内存时优先取用最高 zone
（Normal），只有 Normal 见底才回退到 DMA32，因此档位一从未触及 DMA32 中闲置的
705 个 order-10 完整块（约 1410 个大页），而大页池扩容的 GFP 允许使用 DMA32，最终
全部从该处白拿。加入库存自检后，两轮独立实验测得的库存值分别为 1652 与 1630，
**偏差 1.3%**，压力配方具备确定性。

#### 5. 三层埋点 + kretprobe：direct compaction 的代价度量与结局归因

该模块挂载 `compaction` 子系统的四个 tracepoint 与一个 kretprobe。设计上必须解决
三个层次的粒度不匹配问题：一次 direct compaction 只打一次
`mm_compaction_try_to_compact_pages`（**且只有它携带 `order`**）；其内部按 zone
逐个打 `mm_compaction_begin`/`end`；而每个 zone 内部的迁移主循环每搬运一批就打一次
`mm_compaction_migratepages`。因此实现了**以线程 ID 为键的两层配对**，并对
迁移页数做**累加**而非取最后一次。

其中一项关键设计取舍是：`try_to_compact_pages` **只有入口 tracepoint，没有出口
tracepoint**，而"进程被阻塞了多久"恰恰需要出口时刻。三种备选方案中——

- 以「第一个 `begin` 到最后一个 `end`」近似总耗时：会漏掉函数入口到首个 `begin`
  之间的开销，更严重的是 `status` 为 `DEFERRED`/`SKIPPED` 的尝试**根本不打
  `begin`/`end`**，这类"尚未开始即被劝退"的样本会整体丢失；
- 入口改用 kprobe 通过 `PT_REGS_PARM2` 取 `order`：函数参数顺序并非稳定 ABI；
- **最终方案：入口沿用 tracepoint（稳定 ABI，获取 `order`/`gfp_mask`/`prio`），
  出口挂 kretprobe（仅使用返回值与时间戳，不依赖参数布局）** —— 耦合面最小，
  且 kretprobe 的返回值本身就是 `enum compact_result`，直接给出本次尝试的最终结局。

模块另一个必须做对的点是**三重来源过滤**：`compaction:*` 这组 tracepoint 由
direct compaction、kcompactd 后台线程、管理员手动写 `compact_memory` **三条路径共用**，
不过滤则统计会被后台线程严重稀释。由于只有 direct compaction 会经过
`try_to_compact_pages`，过滤判据被实现为"当前线程是否存在活跃的外层记录"，
**并在内核态完成**，避免为注定丢弃的事件付出 map 写入与数据传输开销。

结局分类覆盖 `enum compact_result` 的**全部 9 个取值**。此处修正了原设计文档的一处
错误：文档仅列出 5 个取值，遗漏了 `COMPACT_PARTIAL_SKIPPED`；若按此实现，该状态会
落入"未知"分类并导致成功率计算失真。该修正经两个独立来源交叉确认（tracepoint
`format` 文件中 `print fmt` 的 `__print_symbolic` 映射表，以及内核头文件
`compaction.h` 的枚举定义）。此外，通过阅读 `compaction.h` 的四个判定函数
（`compaction_made_progress` / `compaction_failed` / `compaction_withdrawn` /
`compaction_needs_reclaim`）明确了**"规整成功率"至少存在三种不同口径**，工具在输出中
显式声明所采用的口径 —— 其中 `COMPACT_SKIPPED` 并非"规整失败"，而是"规整判定自身
不应介入、应先执行回收"，这从代码层面印证了慢路径 retry 循环中
**direct reclaim 先于 direct compaction** 的执行顺序。

延迟数据以 `bpf_log2l` 分桶存入直方图，并**按 `order` 与 `sync` 分维度** ——
不使用平均值，因为规整延迟是典型的长尾分布，平均值会抹平真正造成用户感知卡顿的
那部分尾部样本。

#### 6. 观测工具的自证机制与交叉验证

项目在实施过程中遭遇过一次典型故障：编排脚本因 bash 关联数组下标异常在递增循环
第 10 批静默终止，**恰好停在跨越"必须规整"临界点的前一刻**，而收尾逻辑照常输出
「停止原因：达到目标数量」—— 工具坏了，但报告称自身正常。这促成了一条贯穿全项目的
原则：**观测工具的底线不是"永不出错"，而是"出错时不得伪装成正确"。**

据此建立了多层自证机制：

1. **恒等式校验**：`compact_stall` 必须等于 `compact_success + compact_fail`
   （一次规整要么成功要么失败）。实测在累计值与单轮增量上均成立。
2. **符号与完整性校验**：单调递增计数器不允许出现负增量；每次采样校验所需计数器
   一个不缺，缺失则将该批次标记为不可信。
3. **双路对账**：全程总量另行按「结束绝对值 − 起始绝对值」独立计算一遍，与逐批累加
   互相对账。实测两套算法给出 `compact_stall` 402 与 402、成功率 92.0% 与 92.0%、
   `pgscan_direct` 4409 与 4409，扫描页数偏差 0.1%。
4. **未配对率**：配对表采用 LRU 哈希以避免僵尸表项造成的内存泄漏，但 LRU 淘汰的
   恰是**停留最久**即延迟最长的样本，因此必须输出未配对率作为延迟统计可信度的依据。
5. **静默丢样本检测**：以「直方图样本总数 == 外层退出数 − 未配对数」这一等式检测
   BPF 直方图溢出 —— BCC 的 `increment()` 失败时既不报错也无返回值可查。实现时据此
   发现 `BPF_HISTOGRAM` 两参数形式的默认容量仅 64 项，而二维键最坏需约 330 项。
6. **过滤器自证**：输出被过滤事件的计数，用以证明来源过滤确实在工作，
   而非"恰好没有噪声"。

---

## 量化成果

在 4 核 / 12 GB 的 x86_64 虚拟机（内核 5.15.0-139-generic）上：

| 指标 | 结果 |
|---|---|
| 慢路径可复现性 | 将 `compact_stall` 从长期为 0 稳定提升至单轮 **402 次**，跨多批持续增长 |
| 压力配方确定性 | 两轮独立实验测得空闲块库存 1652 / 1630，**偏差 1.3%** |
| 规整成功率悬崖 | 分配请求仍能被满足时成功率 **92.0%**；一旦分配开始失败则骤降至 **0.0%**，中间无平滑过渡 |
| 无效工作量 | 成功率为 0 的那一轮中，内核隔离了 **179685 个页（约 700 MB）**准备迁移，最终未凑出任何一个 order-9 块 |
| 碎片形态判别 | 空闲侧/迁移侧扫描比在两种压力深度下分别为 **7.36** 与 **0.81**（比值 > 2 表示"空位难找"，< 0.5 表示"可迁移页难找"，后者是 UNMOVABLE 污染的指纹） |
| 交叉验证 | 工具统计与 `/proc/vmstat`、`/proc/pressure/memory` 三方对账，偏差 0.1% |

---

## 简历写法（可直接使用）

### 精简版（3 行，适合一页简历）

> **Linux 物理内存碎片检测与慢路径代价归因工具（eBPF / BCC）**
> - 基于 eBPF 实现零侵入内存碎片监控：kprobe 挂载 `get_page_from_freelist` 采集伙伴
>   系统各 zone/order 空闲块状态，tracepoint 挂载 `mm_page_alloc_extfrag` 归因跨
>   migratetype 降级分配，在内核态以整数运算实现 `extfrag_index` 与
>   `unusable_free_index` 双指标（eBPF 不支持浮点），Python curses 终端可视化。
> - 将工具从"碎片状态观测"升级为"**慢路径代价量化**"：以「入口 tracepoint + 出口
>   kretprobe」的组合度量 direct compaction 对进程的**同步阻塞时长**，两层 tid 配对
>   解决 `order` 仅存于外层埋点的粒度问题，内核态三重来源过滤剔除 kcompactd 与手动
>   规整噪声，覆盖 `enum compact_result` 全部 9 种结局（修正原设计遗漏的
>   `PARTIAL_SKIPPED`）。
> - 自研四档位碎片压力注入器解决"干净机器上无事件可测"的验证前提，将
>   `compact_stall` 从 0 稳定提升至单轮 402 次（两轮库存测量偏差 1.3%）；建立恒等式
>   校验、双路对账、未配对率等自证机制，与 `/proc/vmstat`、PSI 三方交叉验证偏差 0.1%。

### 加分句（面试自我介绍时用）

> 这个项目我做了两轮。第一轮做完我发现它其实很浅 —— 能报出"碎片指数 0.85"，
> 但答不上来"0.85 会让谁慢多少"。我去查机器的 `/proc/vmstat`，
> `compact_stall` 是 0、`pgscan_direct` 是 0、PSI 累计阻塞时间也是 0，
> **说明我监控的这个"碎片"从来没有真正造成过任何后果**。
> 所以第二轮我把它改成量化代价：碎片把分配逼进慢路径之后，
> 进程被同步卡了多久、结局是什么、内核为此做了多少无用功。

### 可预期的追问与准备方向

| 追问 | 准备方向 |
|---|---|
| 为什么要 kretprobe，只用 tracepoint 不行吗？ | 出口时刻 + `DEFERRED`/`SKIPPED` 不打 begin/end |
| 三重来源过滤怎么做的？怎么证明它生效了？ | tid 外层记录做准入 + `begin_reject` 计数自证 |
| 你的成功率是怎么算的？ | 三种口径，明确声明用 `compaction_made_progress` |
| map key 为什么用 tid 不用 tgid？ | compaction 是线程行为，同进程双线程会互相覆盖 |
| 为什么用 LRU hash？代价是什么？ | 换掉了"静默丢长尾样本"，故必须报未配对率 |
| 成功率从 92% 掉到 0% 是怎么回事？ | 压到分配开始失败时，可迁移页耗尽，规整无从下手 |
| 讲一个你踩过的坑 | 工具静默崩溃却报告正常 / DMA32 从未被压到 / stdio 全缓冲 |
