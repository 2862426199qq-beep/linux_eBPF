# 报告_P0：阶段一（P-1 压力注入器 + P0 direct compaction 观测）

| | |
|---|---|
| 阶段 | 阶段一 = P-1 + P0 |
| 提交日期 | 2026-08-14 |
| 实验机 | Ubuntu 20.04（HWE），内核 **5.15.0-139-generic**（上游基线 5.15.178），4 核，12208068 kB RAM |
| 工具链 | BCC 0.12.0，Python 3.8.10 |
| THP | `enabled=[madvise]`，`defrag=[madvise]` |
| 关键结论 | P-1 硬门槛过；P0 真实抓到 377 次 direct compaction，与 `/proc/vmstat` 独立对账一致 |

> **本报告的数据分两类，已逐处标注**：
> - **【原始】** = 直接来自工具输出或 `/proc` 文件
> - **【转录】** = 从 `handoff/com_memory.md` 的实验记录转抄（原始日志在 `/tmp`，机器重启后已丢失）
>
> 丢失原始日志是本阶段的一个实际失误，见第 7 节。

---

## 1. 交付物清单

### 1.1 新增 / 修改的文件

| 文件 | 行数 | 状态 | 说明 |
|---|---|---|---|
| `源码/src/bpf/compactinfo.c` | **487** | 新增（P0 核心） | direct compaction 内核态观测模块，含约 160 行自包含设计注释 |
| `源码/src/extfrag.py` | 625 | 修改 | 新增 `--mode compact` 分支与文本输出（按约定**不新开 Python 文件**） |
| `源码/src/tools/fragstress/holes.c` | 232 | 新增（P-1） | MOVABLE 碎片注入：大量分配后**随机**释放 |
| `源码/src/tools/fragstress/kstack.c` | 201 | 新增（P-1） | UNMOVABLE 污染：创建并**保持存活**大量线程，用内核栈钉住 pageblock |
| `源码/src/tools/fragstress/hugetlb.sh` | 156 | 新增（P-1） | 逐批扩容 hugetlb 池，制造 order-9 需求 |
| `源码/src/tools/fragstress/run.sh` | 538 | 新增（P-1） | 时序编排 + 五道自证防线；**不含任何 eBPF 代码，纯读 `/proc`** |
| `源码/src/tools/fragstress/README.md` | — | 新增 | 复现步骤 + 全部踩坑记录 |

未改动：`fraginfo.c`（207 行）、`extfraginfo.c`（100 行）—— 均为 v1 资产，按约定不动。

### 1.2 怎么跑（完整命令行，顺序不可颠倒）

```bash
# 0) 先在前台过 sudo 密码，否则后台作业会被 SIGTTIN 挂住（踩过）
sudo -v

# 1) 记录基线绝对值（必须用"结束值 − 开始值"，不能信累计值）
grep -E "^(compact_|pgscan_direct|pgsteal_direct|allocstall)" /proc/vmstat > /tmp/vmstat.t0

# 2) ★ 先挂探针。BCC 要现场编译 + 挂载，必须等它真加载完
cd 源码/src
sudo nohup python3 extfrag.py --mode compact --interval 30 --duration 1500 \
     > /tmp/compactinfo.log 2>&1 &
sleep 20

# 3) 再施压
cd tools/fragstress
sudo nohup ./run.sh > /tmp/fragstress_run.log 2>&1 &

# 4) 结束后拷走证据（★ 上一轮就是漏了这步丢的原始日志）
cp /tmp/compactinfo.log /tmp/fragstress_run.log <项目目录>/artifacts/
```

**为什么"探针必须先挂"**：eBPF 探针由内核事件触发，**探针没挂上的那段时间里发生的规整，事后一个都补不回来**。这不是习惯问题，是不可逆的数据丢失。

---

## 2. 埋点实测确认

### 2.1 实际字段（2026-08-02 本机 tracefs 实测）【转录】

本机 `/sys/kernel/debug/tracing/events/compaction/` 下共 **14 个** tracepoint，P0 用其中四个：

| tracepoint | ID | 字段（offset/size 抄自 `format` 原文） |
|---|---|---|
| `mm_compaction_try_to_compact_pages` | 559 | `order` int(8/4)；`gfp_mask` gfp_t(12/4, 无符号)；`prio` int(16/4) |
| `mm_compaction_begin` | 561 | `zone_start`(8/8)、`migrate_pfn`(16/8)、`free_pfn`(24/8)、`zone_end`(32/8) 均 unsigned long；`sync` bool(40/1) |
| `mm_compaction_end` | 560 | 同 begin 五个 + `status` int(**44**/4) |
| `mm_compaction_migratepages` | 562 | `nr_migrated`(8/8)、`nr_failed`(16/8) 均 unsigned long |

**字段名与类型和计划书 §3.2 完全一致**，这部分可以照计划书写。

### 2.2 ★ 与计划书不一致处 1：`status` 实际有 9 个取值，计划书只列了 5 个

两个独立来源交叉确认，结果一致：
- `end/format` 的 `print fmt` 里的 `__print_symbolic` 映射表
- `/usr/src/linux-hwe-5.15-headers-5.15.0-139/include/linux/compaction.h` 的 `enum compact_result`

| 值 | 名字 | 含义 | 计划书有吗 |
|---|---|---|---|
| 0 | `COMPACT_NOT_SUITABLE_ZONE` | 内部值 | — |
| 1 | `COMPACT_SKIPPED` | 空闲页太少，规整判断自己不该上 | ✓ |
| 2 | `COMPACT_DEFERRED` | 因过去连续失败被内核主动退避 | ✓ |
| 3 | `COMPACT_NO_SUITABLE_PAGE` | 内部值 | — |
| 4 | `COMPACT_CONTINUE` | 内部值 | — |
| 5 | `COMPACT_COMPLETE` | 整个 zone 扫完仍没成功（白扫） | ✓ |
| 6 | **`COMPACT_PARTIAL_SKIPPED`** | **扫了一部分就退避** | **✗ 漏了** |
| 7 | `COMPACT_CONTENDED` | 锁竞争，提前终止 | ✓ |
| 8 | `COMPACT_SUCCESS` | 判定分配现在能成功了 | ✓ |

**怎么改的**：代码里 9 个值**全部保留分类**，不做"某些值不会出现"的假设：

```c
status = args->status;
if (status < 0 || status >= STATUS_SLOTS - 1)
        status = STATUS_SLOTS - 1;        // 越界统一归到"未知"槽，不丢弃
c = zone_status.lookup(&status);
if (c) lock_xadd(c, 1);
```

**这个漏项的实测代价是可量化的**：本次实验 `PARTIAL_SKIPPED` 出现 **55 次**（占内层 484 次的 11.4%）。若按计划书那张 5 行表写，这 55 次会掉进"未知"分类，**规整成功率会算错约 11 个百分点**。

### 2.3 ★ 与计划书不一致处 2：`offset` 里有编译器 padding

`end` 的 `sync` 在 offset **40**、size 1，而 `status` 在 offset **44** —— 中间 41/42/43 是编译器为 int 对齐插的空洞。

**照字段列表手抄结构体去解析，会把 `status` 算在 offset 41，读出来全是错值。**

结论：**永远用 BCC 的 `args-><字段名>`（它按 `format` 的 offset 自动生成），永远不要自己算偏移。**

### 2.4 白捡的两张表（在 `format` 的 `print fmt` 里）

- `end/format` 的 `__print_symbolic` = 现成的 status 枚举映射
- `try_to_compact_pages/format` 的 `__print_flags` = **完整的 GFP 标志位表连数值**：`__GFP_DIRECT_RECLAIM=0x400`、`__GFP_KSWAPD_RECLAIM=0x800`、`__GFP_IO=0x40`、`__GFP_FS=0x80`、`__GFP_MOVABLE=0x08`、`__GFP_NORETRY=0x10000`、`__GFP_RETRY_MAYFAIL=0x4000` 等

P0 直接用了 `0x400` 做 `no_direct_reclaim` 自证（见 5.2），不必去翻 `include/linux/gfp.h`。

### 2.5 出口没有 tracepoint —— 用 kretprobe 补

`try_to_compact_pages` **只有入口 tracepoint，没有出口 tracepoint**，而 P0 的核心指标是"这次分配被卡了多久"，没有出口时刻就算不出来。

三个备选方案的取舍：

| 方案 | 判断 |
|---|---|
| 只用 tracepoint，把"第一个 begin 到最后一个 end"当总耗时 | ✗ 漏掉入口到第一个 begin 之间的开销；**更致命的是 `compaction_deferred()` 会 `continue` 跳过 `compact_zone()`，这类尝试一个 begin/end 都不打，会整批丢失** |
| 入口也用 kprobe，`PT_REGS_PARM2` 取 `order` | ✗ 参数在哪个寄存器不是稳定 ABI，换内核版本可能读到垃圾，**且不会报错** |
| **入口用 tracepoint（稳定 ABI 拿 order/gfp/prio），出口用 kretprobe（只取返回值和时刻）** | ✓ **采用**。耦合面最小，只依赖"该函数存在且非 inline"。实测它在 `/proc/kallsyms` 里是全局符号 `T` |

方案二的源码依据（`mm/compaction.c` v5.15.178，`try_to_compact_pages()` 内）：

```c
for_each_zone_zonelist_nodemask(...) {
        if (prio > MIN_COMPACT_PRIORITY
                        && compaction_deferred(zone, order)) {
                rc = max_t(enum compact_result, COMPACT_DEFERRED, rc);
                continue;                      // ★ 跳过，没进 compact_zone
        }
        status = compact_zone_order(...);      // begin/end 在这里面才会打
```

额外收益：**kretprobe 的返回值就是 `enum compact_result`**，即这次尝试的最终结局，白拿。

---

## 3. 压力注入效果（阶段一必须项）

### 3.1 验收标准与结果

计划书 §2.4 的硬门槛是 **`compact_stall` 必须涨**。

| | 结果 |
|---|---|
| P-1 首轮（2026-08-11） | `compact_stall` 增量 **402**，跨 3 批持续增长 |
| P0 验证轮（2026-08-12） | `compact_stall` 增量 **377** |
| **门槛** | **通过** |

对照基线：这台机器开机至今 `compact_stall = 0`、`pgscan_direct = 0`、`/proc/pressure/memory` 全 0（项目开工时实测的"三个 0"）。**压力注入是从 0 造出这些事件的。**

### 3.2 `/proc/vmstat` 关键计数器（2026-08-12 P0 验证轮，增量值）【转录】

由 `run.sh` 采集，**与 eBPF 完全独立，该脚本不含一行 eBPF 代码**：

| 计数器 | 增量 | 说明 |
|---|---|---|
| `compact_stall` | **377** | 分配因要连续内存被同步卡住的次数 |
| `compact_success` | 377 | |
| `compact_fail` | 0 | |
| `compact_daemon_wake` | 86 | kcompactd 被唤醒（**这是 eBPF 侧噪声的独立佐证**） |
| `compact_isolated` | 1140569 | 为搬迁而隔离的页 |
| `compact_migrate_scanned` | 1615448 | |
| `compact_free_scanned` | 6209849 | |
| `pgscan_direct` | **0** | 直接回收扫描 —— 一次都没发生 |
| `pgsteal_direct` | **0** | |
| `allocstall_movable` | 0 | |
| PSI `some` / `full` | **694 ms / 689 ms** | |

**恒等式自检通过**：`compact_stall 377 == compact_success 377 + compact_fail 0`。

**free/migrate 扫描比 = 6209849 / 1615448 = 3.84**（>2：空闲侧要翻很远才找到落脚点，即"空位难找"）。

### 3.3 逐批推进（证明压力是渐进生效的，不是一次性噪声）【转录】

```
批 1  stall=0     ┐
批 2  stall=0     │ 白拿区：库存里还有现成的高阶块，直接摘走，不进慢路径
批 3  stall=0     │
批 4  stall=0     ┘
批 5  stall=95    ← ★ 越过库存线，规整立刻出现
批 6  stall=134
批 7  stall=148
             合计 = 95 + 134 + 148 = 377   ✓ 与总量对账一致
```

**库存自检成功"预测"了现象出现的时机。** `run.sh` 在开跑前先算了一次可白拿库存（本轮 826），据此预测批 1–4 是白拿区；实测正是如此，越过库存线的批 5 立刻出现 95 次规整。

> **一个能预测自己什么时候会看到现象的实验，比"碰巧看到了"强得多。**

这个库存自检不是一开始就有的，它是第二次失败逼出来的（见 7.2）。

### 3.4 `/proc/pagetypeinfo` 的 Unmovable 行变化 —— **数据缺失，如实说明**

计划书要求本节给出 `pagetypeinfo` 的 Unmovable 行前后对照。**这一项本报告给不出完整数据**：

- 手上只有 2026-08-09 单独测 `kstack.c` 时的数字：**Normal zone 的 Unmovable pageblock 从 287 涨到 393（+106，12000 线程）**【转录】
- **2026-08-12 P0 验证轮的 `pagetypeinfo` 快照随 `/tmp` 一起丢了**

所以"UNMOVABLE 污染在 P0 验证轮里达到了什么程度"**目前无法回答**。这是本阶段的一个实际缺口，补法见 7.4。

### 3.5 一个有解释力的对照：成功率悬崖【转录】

P-1 阶段跑了两轮，**唯一差别是压到多深**：

| | 停在"每批还能拿满" | 压到"分配开始失败" |
|---|---|---|
| `compact_stall` 增量 | 402 | 42 |
| **规整成功率** | **92.0%** | **0.0%** |
| `free/migrate` 扫描比 | 7.36 | **0.81** |
| `compact_isolated` | — | 179685（隔离约 700 MB 准备搬，**一个 order-9 都没凑出来**）|

**只要分配还能被满足，规整基本都成功；一旦分配开始失败，规整完全失效。中间没有平滑过渡。**

扫描比从 7.36 翻转到 0.81 给出了机制解释：**瓶颈从"空位难找"变成了"搬得动的页难找"** —— 可迁移的 MOVABLE 页已被消耗殆尽，剩下的几乎全是内核栈、slab 和已分配大页，规整搬无可搬。

> 同一套工具在两种压力深度下给出**方向相反**的比值，说明它在测量物理现象，不是在输出常量。

**复现性**：两轮独立测得的"白拿库存"1652 vs 1630，**相差 1.3%**。

---

## 4. 交叉验证表

### 4.1 三列并排

| 指标 | eBPF（`compactinfo.c`） | `/proc/vmstat`（`run.sh`） | PSI | 偏差与解释 |
|---|---|---|---|---|
| direct compaction 次数 | `outer_enter` = **377** | `compact_stall` = **377** | — | **完全一致**（但不是恒等式，见 4.2）|
| 外层退出次数 | `outer_exit` = **377** | — | — | 进出相等 → 未配对 0 |
| 规整成功 | 外层返回值 SUCCESS = 376（**99.7%**）| `compact_success` = 377（**100.0%**）| — | **差 1 次，已定位到具体机制，见 4.3** |
| per-zone 次数 | `begin_accept` = **484** | — | — | 484 > 377：内层是 per-zone 粒度，一次尝试平均遍历 **1.28** 个 zone |
| 被过滤的噪声 | `begin_reject` = **171**<br>`migratepages` reject = **2075** | `compact_daemon_wake` = **86** | — | vmstat 独立证实 kcompactd 确实活动过 → 被挡掉的事件真实存在，**不是过滤器乱挡** |
| 单次阻塞时长 | 直方图众数 **1024–2047 μs** | — | 694 ms / 377 次 = **1.84 ms/次** | **数量级一致**。且本轮 `pgscan_direct = pgsteal_direct = 0`，PSI 的内存阻塞时间**几乎全部来自规整**，所以这个平均值可比 |
| 迁移页数 | 成功 166973 / 失败 8071（失败率 **4.6%**）| `compact_isolated` = 1140569 | — | 量级不同属正常：`isolated` 统计的是"隔离出来准备搬"的页，多于实际搬成的 |

### 4.2 ★ 对账等式的精确形式（本报告的一处自我修正）

初稿曾把 `outer_enter == compact_stall` 写成恒等式。**核对源码后发现不成立。**

`count_vm_event(COMPACTSTALL)` 不在 `try_to_compact_pages()` 里，而在它的调用者 `__alloc_pages_direct_compact()` 里，**且前面有一个早退**（`mm/page_alloc.c` v5.15.178）：

```c
	*compact_result = try_to_compact_pages(gfp_mask, order, alloc_flags, ac,
							prio, &page);
	...
	if (*compact_result == COMPACT_SKIPPED)
		return NULL;                          // :4409  ★ 早退，不计数
	count_vm_event(COMPACTSTALL);                 // :4410
	...
	if (!page)
		page = get_page_from_freelist(...);   // :4418  搬完再试一次
	if (page) {
		count_vm_event(COMPACTSUCCESS);       // :4425  ★ 拿到页才算成功
		return page;
	}
	count_vm_event(COMPACTFAIL);                  // :4433
```

**精确等式：**

```
compact_stall  ==  outer_exit  −  (返回值 == COMPACT_SKIPPED 的次数)
```

本轮 `attempt_status` 里 **`SKIPPED` 出现 0 次**（只有 SUCCESS 376 + CONTENDED 1），所以减数为 0，等式退化成 377 == 377。

**这意味着**：内存真正紧张、`SKIPPED` 大量出现时，`compact_stall` 会**明显小于** `outer_enter`。届时若仍按"应该相等"去判断，会误判成工具有 bug。**数据是对的，初稿给的理由不完整。**

### 4.3 那 1 次差异的完整剧情（eBPF 99.7% vs vmstat 100.0%）

```
某次 order-9 分配
  → try_to_compact_pages() 返回 COMPACT_CONTENDED（锁竞争，提前撤）
       → eBPF 记录：规整失败                        ← 99.7% 里缺的那 1 次
  → 返回值不是 SKIPPED，所以 :4410 compact_stall++
  → :4418 get_page_from_freelist —— ★ 从别的地方捡到了页
       → :4425 COMPACTSUCCESS++
       → vmstat 记录：成功                          ← 100.0%

  规整失败，分配成功。两个计数器都没错。
```

**根因：两者问的不是同一个问题。** vmstat 的 `compact_success` 加在 `:4418` 那次 `get_page_from_freelist` **拿到页之后**，所以它答的是"**分配最终拿到页了吗**"，不是"规整成功了吗"。

### 4.4 ★ 由此引出：本工具报的成功率是哪一种

同一批数据能算出**四种**成功率，全都对，因为它们问的不是同一个问题（2×2：粒度 × 判据）：

| 口径 | 定义来源 | 算法 | 结果 |
|---|---|---|---|
| 内层严格 | `compaction_made_progress()`（只认 `COMPACT_SUCCESS`）| 373/484 | **77.1%** |
| 内层宽松 | 非 `compaction_failed()`（只有 `COMPACT_COMPLETE` 算真输）| (484−51)/484 | **89.5%** |
| **外层返回值** | `try_to_compact_pages()` 的返回值 | 376/377 | **99.7%** ← **本工具报这个** |
| vmstat | `compact_success / compact_stall` | 377/377 | **100.0%** |

**选 99.7% 的理由**：P0 测的是**代价**，主角是**那次被卡住的分配**，所以粒度必须对齐到"一次尝试"；判据用规整自身的返回值，因为要衡量的是**规整这个手段管不管用**。

那三堆值的划分不是我定的，是内核 `include/linux/compaction.h` 自己划的：

```c
compaction_made_progress()  → 只有 COMPACT_SUCCESS
compaction_failed()         → 只有 COMPACT_COMPLETE          （唯一的"真输"）
compaction_withdrawn()      → DEFERRED / CONTENDED / PARTIAL_SKIPPED  （"退避"，还能再试）
compaction_needs_reclaim()  → 只有 COMPACT_SKIPPED
```

**"退避"这一堆既不算成功也不算失败**，正是严格口径和宽松口径的差额。

### 4.5 内层结局分布【原始输出转录】

```
外层进入 / 退出            377 / 377
内层 begin 接纳 / 被过滤   484 / 171
内层 end   接纳            484
migratepages 接纳 / 被过滤 1063 / 2075
外层结局：SUCCESS 376 (99.7%)   CONTENDED 1 (0.3%)
内层结局：SUCCESS 373   PARTIAL_SKIPPED 55   COMPLETE 51   CONTENDED 5   （合计 484 ✓）
累计迁移页：成功 166973 / 失败 8071   → 迁移失败率 4.6%
```

**`migratepages` 有 2/3 是噪声（2075/3138）。** 不做来源过滤，"平均迁移多少页"会被 kcompactd 的后台活动污染成约三倍。

### 4.6 核心交付数据：代价有多大（`/proc/vmstat` 永远给不出）

```
每次 direct compaction 的总延迟   order = 9（2 MB）
        512 ~ 1023   μs |  50
       1024 ~ 2047   μs | 141   ← 众数
       2048 ~ 4095   μs | 132
       4096 ~ 8191   μs |  31
       8192 ~ 16383  μs |   8
      16384 ~ 32767  μs |   5
      32768 ~ 65535  μs |   1   ← 最坏一次 65 毫秒
```

**一句话**：**碎片让每次 2 MB 分配同步阻塞 1~2 ms（众数），P99 约 16 ms，最坏 65 ms。**

这正是 v1 答不出、v2 能答的那个"所以呢"：v1 只能说"碎片指数 0.87"，v2 能说"因此有 377 次分配被卡住，中位数 1~2 ms，尾部 65 ms"。

> ⚠️ **记录缺口**：上表转录的各桶之和为 **368**，而工具的自检等式报的样本数是 **377**。差的 9 个样本应在 512 μs 以下的桶里，转录时漏抄了。原始日志已随 `/tmp` 丢失，**无法补回**。等式本身当时是通过的（见 5.1），但本报告展示的直方图不完整。

### 4.7 ★ 一个只能由源码解释的观测：`377 stall / 0 pgscan_direct`

乍看矛盾：慢路径是"先回收后规整"，怎么可能规整 377 次而直接回收 0 次？

**解释**：hugetlb 池扩容要 **order-9 > 3 = costly order**，走的是 `__alloc_pages_slowpath()` **retry 循环之前**那一发提前规整（`page_alloc.c:5013–5017`）：

```c
	if (can_direct_reclaim && can_compact &&
			(costly_order ||
			   (order > 0 && ac->migratetype != MIGRATE_MOVABLE))
			&& !gfp_pfmemalloc_allowed(gfp_mask)) {
		page = __alloc_pages_direct_compact(...);   // :5017
		if (page)
			goto got_pg;                       // ★ 直接返回，没进 retry 循环
	}
```

规整成功率 99.7%，拿到页就直接 `goto got_pg` 返回了，**retry 循环压根没进去**，所以循环里的 `__alloc_pages_direct_reclaim()`（:5092）一次都没被调用。

> **这组数字只能由"规整发生在回收之前"来解释。** 它把内核对 costly order 的特殊处理**实测出来了**，不是从注释里读出来的。
>
> 对照：order-2（非 costly、且是 MOVABLE）**不可能**出现这个组合，它会先付回收的代价。

---

## 5. 未配对率

### 5.1 实测

| 项 | 数值 |
|---|---|
| 外层进入 / 退出 | 377 / 377 |
| **未配对率** | **0.00%** |
| 直方图样本数自检 | **377 == 377 − 0** ✓ |

**为什么必须报这个数**：配对表用的是 `BPF_TABLE("lru_hash", ...)` 而非普通 hash。这是一笔**有代价的交易**：

```
普通 hash：僵尸表项（进去了没出来）永不清理 → 表满 → 新事件写入直接失败 → 内存泄漏
LRU hash：表满时淘汰最久未用的表项 → 不泄漏
```

**但被淘汰掉的恰恰是停留最久的表项 = 延迟最长的样本，而长尾正是性能问题的本体。**

所以 LRU 是"拿内存泄漏风险换静默丢事件"。**不报未配对率的延迟统计不可信** —— 因为丢掉的不是随机样本，是系统性偏向长尾的样本，剩下的数据会让延迟看起来比真实情况**好**。

本轮 0.00%，因此第 4.6 节的延迟数据可用。

（BCC 0.12.0 没有 `BPF_LRU_HASH` 便捷宏，实测确认；用底层 `BPF_TABLE("lru_hash", ...)`，效果相同。）

### 5.2 四道自证机制

| 机制 | 抓什么 | 本轮结果 |
|---|---|---|
| `outer_unpaired` / `end_unpaired` | LRU 淘汰导致的长尾样本丢失 | **0 / 0** |
| 直方图样本数 == 外层退出 − 未配对 | 抓"静默丢样本"（BCC 的 `increment()` 失败时不报错、无返回值可查）| **377 == 377 ✓** |
| `begin_reject > 0` | 证明来源过滤器真在工作，而非恰好没噪声 | **171 > 0 ✓** |
| `no_direct_reclaim` 预期恒为 0 | direct compaction 必然带 `__GFP_DIRECT_RECLAIM(0x400)`。一旦非 0，说明我们对这条路径的理解有偏差 | **0 ✓** |

**关于第三道**：`reject = 0` 有两种可能 —— 过滤器坏了，或真的没噪声。**不计数就分不清这两者**，所以被丢弃的事件也必须计数。

**顺手抓到的一个真隐患**：`BPF_HISTOGRAM(名字, key类型)` 两参数形式展开成 `max_entries = 64`。本模块的 key 是二维的（order × log2 桶），最坏 11 × 30 = 330 项，**超出后 `increment()` 会静默失败**：不报错、没有返回值可查、丢掉的样本无声无息。改成三参数形式显式开 1024，并由上面第二道等式兜底。

### 5.3 未配对的三个来源（代码里逐条列出，不静默丢弃）

1. 进程在探针加载之前就已进入该函数（只在启动瞬间出现）
2. `lru_hash` 淘汰了表项（map 压力大时）
3. **结构性来源**（2026-08-12 核对 `mm/compaction.c` 才发现）：

```c
	if (!gfp_compaction_allowed(gfp_mask))
		return COMPACT_SKIPPED;                        // ← 早退
	trace_mm_compaction_try_to_compact_pages(...);         // ← 埋点在后
```

**早退发生在入口 tracepoint 之前**，而 kretprobe 挂在函数返回上、不管从哪条路返回都会打 → **必然产生"有出口无入口"的未配对**。这不是偶发噪声，是这条路径的固有行为。

本轮 `unpaired = 0`，因为 hugetlb 的 GFP 允许规整，从没走这条早退。**换个 caller 就会出现，届时不要误判为探针出错。**

> 由此得到一条一般性要求：**观测工具的每一个"未配对/异常"计数器，都应该能列出它的全部来源。列不全，说明还没读透那段内核代码。**

---

## 6. 内核原理复述

> 这一节是用自己的话讲清 P0 挂的埋点背后内核在干什么。阶段一的要求是讲**双扫描器**和**迁移失败的原因**。

### 6.1 规整为什么存在

伙伴系统里，一个 order-n 的块要能被凑出来，前提是**若干物理上互为伙伴的页同时空闲**。合并发生在 `__free_one_page()`：每释放一个页，内核立刻检查它的伙伴（`buddy_pfn = pfn ^ (1 << order)`）是否也空闲，是就当场合并升阶。

注意合并要求的是**伙伴关系**，不是"地址挨着"。order-0 的 PFN 1 和 2 相邻，但 1 的伙伴是 0、2 的伙伴是 3 —— 两个都空闲也不会合并。

所以会出现这样的局面：**空闲总量绰绰有余，但没有任何一组伙伴同时空闲**，于是高阶分配失败。这就是碎片化。

**规整（compaction）是对这个局面的补救：把还在用的页搬走，腾出物理上连续的一片。**

关键的一句是：**规整不创造内存，它只是搬家。** 搬家需要目标空位，所以空闲页太少时规整根本运作不起来——这正是慢路径里"先回收、后规整"的原因，也是 `COMPACT_SKIPPED` 这个返回值的含义。内核在 `page_alloc.c:5121` 把这个依赖写死了：**回收没有进展，连重试规整的资格都没有**。

### 6.2 双扫描器：从哪来、到哪去

规整的目标是**把还在用的页往 zone 的高地址端赶，腾空低地址端**。所以有两个扫描器，相向而行：

```
zone 低地址 ──────────────────────────────────────► zone 高地址

  migrate 扫描器 ──►                        ◄── free 扫描器
  从低往高爬                                    从高往低走
  找「搬得动的页」                              找「空闲页」（落脚点）
                    ────► 页往这个方向搬 ────►

              两者相遇 → 本轮规整结束
```

**为什么必须相向**：两个指针一碰头，整个 zone 恰好被覆盖一遍，不重不漏 —— 这给了"扫完了"一个自然的定义。同向走就没法判断什么时候该停。

**一个容易漏的实现细节**：free 扫描器是**按需驱动**的。`isolate_freepages()` 在 `mm/compaction.c` 里只有一个调用点（:1688），位于 `compaction_alloc()` —— 也就是 `migrate_pages()` 的 `get_new_page` 回调 —— **只有 `cc->freepages` 空了才会去补货**。而 `compact_zone()` 的主循环开头是 `switch (isolate_migratepages(cc))`，一旦返回 `ISOLATE_NONE` 就 `goto check_drain`，**连迁移都不做，free 扫描自然也不会发生**。

所以两侧的扫描量不是对称的，它们的比值反而成了一个有信息量的指纹：

> **哪一侧要找的东西稀缺，那一侧就得翻很远，它的扫描数就爆。**

| `free/migrate` | 含义 |
|---|---|
| **> 2** | 空闲侧翻得远 → **空位难找**（内存被占满，压力温和到中等）|
| ≈ 1 | 两侧对称相遇 |
| **< 0.5** | 迁移侧翻得远 → **搬得动的页难找**，UNMOVABLE 占比高（极端压力）|

本轮实测 3.84（空位难找）；P-1 压到分配失败那轮是 0.81（搬无可搬）。**同一个指标在两种压力下方向相反**，这比单点数值有用得多。

### 6.3 迁移为什么会失败（本轮 4.6%）

`nr_failed` 统计的是**隔离出来了、但没搬成**的页。常见原因：

1. **页根本搬不动（migratetype 决定）** —— 内核栈、slab、页表这类 UNMOVABLE 对象没有"改个映射就换个物理页"的机制，内核不知道谁持有指向它的指针。**这类页一个就能钉死整个 pageblock**：把周围全搬干净也没用，那个 pageblock 永远凑不成高阶块。
2. **页被临时钉住（pinned）** —— 正在做 DMA、被 `get_user_pages()` 引用、引用计数对不上，此刻不能动。
3. **锁竞争** —— 异步迁移遇到 `lru_lock` / `zone->lock` 竞争就主动放弃（`COMPACT_CONTENDED`），不等。同步迁移会等。这就是延迟直方图要按 `sync` 分维度的原因：两者成本天差地别。

**本机的 UNMOVABLE 来源和计划书假设的不一样。** 计划书假设内核栈是 order-2 连续块，实测 `CONFIG_VMAP_STACK=y` —— 内核栈是 vmalloc 出来的、**虚拟地址连续 16 KB 但物理上是 4 个各自独立的 order-0 页**。证据有两条：配置侧 `grep VMAP_STACK /boot/config-$(uname -r)` = y；行为侧 `KernelStack` 涨了几千 kB 而 `/proc/buddyinfo` 的 order-2 空闲块数没有对应减少。

**机制不同，破坏效果反而更狠**：它不是消耗 order-2，而是往大量 pageblock 里各钉一颗搬不动的钉子 —— **钉子越分散，被钉死的 pageblock 越多**。

### 6.4 三个埋点的粒度不同，这直接决定了代码结构

```
一次 direct compaction（= 一个进程被卡住一次）
└── try_to_compact_pages           ← 1 次，外层边界，★ 唯一带 order
    ├── begin(zone A) … end(zone A)      ← per-zone
    │   ├── migratepages                 ← per 迁移批次，主循环每搬一批打一次
    │   └── migratepages …
    └── begin(zone B) … end(zone B)
```

三条推论，每条都对应代码里的一个决定：

1. **`order` 只有最外层有** → `begin/end` 能测出"这个 zone 花了 5 ms"，但说不出这 5 ms 是为了凑多大的块。而 order-3（32 KB）和 order-9（2 MB）的规整难度差一个量级，混在一张表里算平均等于什么都没测。→ **必须两层配对**：外层把 `{ts, order}` 存进 map，出口再取出来。
2. **`begin` 次数 ≠ `compact_stall`**，而且**两个方向都会偏**（一次尝试可遍历多个 zone → 偏多；zone 被 `compaction_deferred` 跳过则一个 begin 都不打 → 偏少）。误差没法用系数校正。→ **能和 `/proc/vmstat` 对账的只有外层。**
3. **`migratepages` 在一次 begin/end 内会打多次** → `nr_migrated/nr_failed` 必须**累加**，取最后一次会严重低估。

补一句实现上的事实：eBPF 的探针之间**不共享变量**（每次触发都是独立的调用，栈是新的），探针间传数据**只有一条路——写进 map、另一个探针读出来**。上面第 1 条的"两层配对"就是这个机制的直接后果。

### 6.5 为什么必须过滤来源

`compaction:*` 这组 tracepoint 是**三条路径共用**的：进程自己被卡住（direct）、kcompactd 后台整理、管理员 `echo 1 > /proc/sys/vm/compact_memory`。三者最后都跑进同一个 `compact_zone()`，**打出来的事件没有任何字段能区分来源**。

区分点在于**怎么走进来的**。查过 `compact_zone()` 的全部四个调用点，只有一条经过 `try_to_compact_pages()`：

| 调用者 | 来源 | 经过 `try_to_compact_pages` |
|---|---|---|
| `compact_zone_order()` ← `try_to_compact_pages()` | direct | **✅ 只有这一条** |
| `proactive_compact_node()` | kcompactd | ❌ |
| `kcompactd_do_work()` | kcompactd | ❌ |
| `compact_node()` ← `sysctl_compaction_handler()` | 管理员 | ❌ |

所以内层探针的准入条件就是"**当前 tid 有没有活跃的外层记录**"—— 外层探针在 map 里留下的那条记录，本身就是"这是 direct compaction"的凭证。

**过滤必须在内核态做**：捞到用户态再扔，等于白付一次 map 写入加一次 perf 传输的开销，而本轮噪声占了 2/3。

**顺带一个实用结论**：`echo 1 > /proc/sys/vm/compact_memory` **不能**用来验证 direct compaction。不是因为它异步（它其实是同步的），而是因为它绕开了 `try_to_compact_pages()`，后果两头都错 —— **`compact_stall` 不涨**（那个计数器在慢路径里加，所以它没法当验收指标），**但 begin/end 照常打**（只挂内层的工具会把它误收进来）。

---

## 7. 偏离与遗留

### 7.1 与计划书的偏离

| # | 计划书怎么写 | 实际怎么做 | 为什么 |
|---|---|---|---|
| 1 | `status` 5 个取值 | 代码保留 **9 个**全部分类 | 实测 `COMPACT_PARTIAL_SKIPPED(6)` 存在且本轮出现 55 次（占 11.4%），按 5 行表写会算错成功率 |
| 2 | 内核栈是 order-2 连续块 | 实测 `CONFIG_VMAP_STACK=y`，是 4 个不相邻 order-0 页 | 见 6.3。`kstack.c` 的破坏机制解释相应改写 |
| 3 | （未提及）只用 tracepoint | 出口加 **kretprobe** | `try_to_compact_pages` 没有出口 tracepoint；纯 tracepoint 方案会整批丢掉 DEFERRED 类样本，见 2.5 |
| 4 | （未提及）扫描页数 | **没有**用 `free_pfn - migrate_pfn` 硬算 | 两个扫描器会重启，硬算会得出负数或荒谬巨值。扫描量的权威来源是 `/proc/vmstat` |
| 5 | 成功率单一口径 | 明确声明用**外层返回值**口径（99.7%），并列出全部四种 | 见 4.4 |

### 7.2 P-1 阶段的四次失败（根因，不是"改了个 bug"）

| # | 现象 | 根因 |
|---|---|---|
| 1 | 手工执行"成功"了但过程失控 | 网卡闪断 → SIGHUP → 压力源被杀，**而实验还在跑，数据静悄悄失真**；且人肉掐时序不可靠 → 催生 `run.sh` |
| 2 | `compact_stall` 全程 0 | **目标值定低了**。匿名页优先从 Normal 拿，`holes` 天然够不着 DMA32，而 hugetlb 的 GFP 允许用 DMA32 → 2000 个大页全从 DMA32 白拿走了。差 88 个就跨过门槛 → 催生**库存自检**（先算库存，再把目标顶到库存之上）|
| 3 | **脚本在出结果前一刻静默崩掉，还报告说自己正常** | bash nameref 数组下标错误让循环在第 10 批终止（停在 1800 个大页，库存 1851 —— 死在"再要一批就必须规整"的前一刻），而收尾逻辑照常打印"达到目标数量 2791" → 催生**五道自证防线** |
| 4 | 成功 | 干净数据，402 次 |

第 3 条是观测类项目最要命的失效模式：**工具坏了，但它报告说自己好着呢。** 由此定下本项目的原则：

> **观测工具的底线不是"永不出错"，是"出错时不许伪装成正确"。**

`run.sh` 的五道防线（采样完整性、符号校验、恒等式校验、双路对账、退出自证）和 `compactinfo.c` 的四道自证（5.2）都是这条原则的产物。

### 7.3 未解决的问题

1. **`/proc/vmstat` 异常读数的机制至今未查清。** 曾出现单调递增计数器给出 **−447** 增量、某增量恰好等于该计数器绝对值。
   - 提出的假设：`/proc/vmstat` 是 seq_file，内容超过一页会分块生成，bash 的 `while read` 可能读到跨越两次生成、不自洽的快照。
   - **设计了实验并自己把它证伪了**：造每秒 2.5 万次 pgfault 的负载，`while read < /proc/vmstat` 连读 200 次，**异常 0 次**。
   - 对策改成**不依赖机制**的四道防线 —— 既然查不出根因，就设计成"无论什么原因导致的坏数据都能被抓出来"。

2. **成功率悬崖只复现过一次。** 92% → 0% 那组数据来自 P-1，P0 验证轮没有复现（目标值没顶到分配失败）。要复现需把 `STALL_GOAL` 调高或把目标推到内存上限。

3. **`pagetypeinfo` 的 Unmovable 前后对照缺失**（见 3.4）。

4. **一个 P-1 留下的开放问题尚未回答**：每次 stall 的平均 PSI 时间，失败那轮 177.8/42 = **4.2 ms**，成功那轮 4691/402 = **11.7 ms** —— **失败的规整反而更便宜**。合理猜测是失败路径提前退出（DEFERRED/SKIPPED），但要 per-attempt 延迟直方图**按 `status` 分维度**才能证实。**现在不写进结论。**

5. **延迟直方图转录不完整**（各桶之和 368 vs 自检值 377，见 4.6）。

### 7.4 流程上的失误（本阶段最该改的一条）

**原始日志放在 `/tmp` 且没有拷出来，机器重启后全部丢失。** 后果是第 3.4 节和 4.6 节的数据缺口**无法补回**，只能靠转录。

**已改**：1.2 节的运行步骤里加了第 4 步 `cp` 到项目 `artifacts/`。**下一轮实验必须先建这个目录再开跑。**

### 7.5 下一阶段（P1）的风险

| 风险 | 说明 | 预案 |
|---|---|---|
| **本机跑不出 direct reclaim** | 本轮 `pgscan_direct = pgsteal_direct = 0` —— 现有压力策略**一次直接回收都没触发**，因为 order-9 是 costly，走的是 retry 循环之前那一发规整就返回了 | P1 需要**新的压力形态**：要么申请非 costly 的高阶（order 1~3）逼它进 retry 循环，要么直接压低 `MemFree` 触发回收。**这是 P1 开工前必须先解决的前置门槛，性质等同于 P-1 之于 P0** |
| 埋点差异 | `vmscan:` 那两个 tracepoint 的字段尚未在本机实测 | 开工第一步先 `cat format`，不照计划书写 |
| 骨架复用的陷阱 | P1 计划复用 P0 的两层配对骨架，但 direct reclaim 的层次结构未必同构 | 先画出 `vmscan` 的调用层次和埋点粒度，确认能不能复用，**不能就不复用** |
| 归因粒度 | reclaim 是 per-node/per-zone，与 compaction 的粒度不同 | 对账时必须写清是哪一层的计数（P0 已踩过这个坑）|

---

## 附：本报告用到的源码位置

内核源码取自 `git.kernel.org` 上游 **v5.15.178** 标签（与本机 `/proc/version_signature` 的上游基线一致），放在 **git 仓库之外**的 `/home/xxy/wlsp/ksrc-5.15.178/`：

```bash
D=/home/xxy/wlsp/ksrc-5.15.178/mm; mkdir -p $D
B='https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/plain'
for f in page_alloc.c compaction.c vmscan.c internal.h; do
  curl -sS -m 90 -o "$D/$f" "$B/mm/$f?h=v5.15.178"
done
```

> 走过一段弯路：先试了 `apt-get install linux-hwe-5.15-source-5.15.0`，装完 `dpkg -L` 只有 `changelog` 和 `copyright` —— 是个**空壳包**。事前 `apt-cache show` 里的 `Size: 74244`（74 kB）就该看出来，真内核源码是 100 MB 量级。
> **教训：装包前看 `apt-cache show` 的 Size，装完用 `dpkg -L` 验收。** 而且 curl 这条路更好——版本能精确对上（发行版仓库里的是 -126，比运行中的 -139 落后 13 个 ABI 版本）。

`include/linux/compaction.h` 用的是本机可读的 `/usr/src/linux-hwe-5.15-headers-5.15.0-139/include/linux/compaction.h`。
